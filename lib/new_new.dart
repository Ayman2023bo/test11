import 'package:flutter/material.dart';

class New_new extends StatelessWidget {
  const New_new({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}